apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: zarr-fuse-ingress
  annotations:
    {{- toYaml .Values.ingress.annotations | nindent 4 }}
  labels:
    {{- toYaml .Values.ingress.labels | nindent 4 }}
spec:
  ingressClassName: {{ .Values.ingress.className | quote }}
  rules:
    {{- range .Values.ingress.hosts.ingressService }}
    - host: {{ .name | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ $.Values.ingressService.service.name | quote }}
                port:
                  name: {{ $.Values.ingressService.service.externalPort.name | quote }}
    {{- end }}
    {{- range .Values.ingress.hosts.extractorService }}
    - host: {{ .name | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ $.Values.extractorService.airflow.webServer.service.name | quote }}
                port:
                  name: {{ $.Values.extractorService.airflow.webServer.service.externalPort.name | quote }}
    {{- end }}
  tls:
    {{- range .Values.ingress.hosts.ingressService }}
    {{- if .tls.enabled }}
    - secretName: {{ .tls.secretName | quote }}
      hosts:
        - {{ .name | quote }}
    {{- end }}
    {{- end }}
    {{- range .Values.ingress.hosts.extractorService }}
    {{- if .tls.enabled }}
    - secretName: {{ .tls.secretName | quote }}
      hosts:
        - {{ .name | quote }}
    {{- end }}
    {{- end }}
