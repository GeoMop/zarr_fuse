name: Ingress server - pull request
on:
  pull_request:
    branches:
      - "*"
jobs:
  build:
    uses: ./.github/workflows/ingress-server-reusable-workflow.yaml
    with:
      push-container: false
      deploy-helm-chart: false
    secrets: inherit
